# Mentor Daily Note — 2026-08-15 (07:00 MYT slot, mentor-ai-daily)

> **Adaptive override triggered (consistent with 2026-08-12, 08-13 PM, 08-14 runs).** The conceptual ladder is **COMPLETE** (8 rungs: stack → tool/agentic → guardrails → evals → precision/recall → golden set → safe rollout → drift & continuous re-audit). The student is a cron target (no interactive answers — all prior tests still pending), so teasing a contrived 9th concept would be busywork. Today's run **ships the one standing execution artifact the mentor can still fill without human sign-off: the PENSOLAR agent/tool surface** (`auto_schedule_crew`) — the actual POC-build shape that the 2026-08-11 "define 3 must-have tools" gap asked for and never got, and the prerequisite for next_action #2 (wire `score_eval.py` to live output). This is an **execution** run, not a new-concept run.

## What we built (the gap, closed)
- `by_industry/solar_energy/pensolar/modules/agent_surface.md` — the concrete **tool surface + agent loop** for the PENSOLAR POC. It turns the abstract "stack + agent + guardrails" ladder into a buildable 6-tool spec grounded in the real P1–P6 workflow.
- **The 6 tools, with guardrail class:** `get_permit_status` / `get_project_state` / `check_document_gate` (all read-only, low risk) · `flag_block_risk` (THE guardrail — its `should_block` output *is* the `golden_v1.csv` column score_eval tests) · `propose_schedule` (advisory/reversible) · `commit_schedule` (the ONLY irreversible action, recall-gated).
- **The agent loop:** goal = "install-ready on time without a compliance miss"; reads state → checks doc gate → flags block (escalates, never auto-overrides) → proposes schedule → acts per dial → self-corrects on slip. Predefined paths (workflow) where possible, dynamic (agent) only for reschedule-on-slip — exactly the R2 "start with tool use" principle.
- **Why it unblocks the ladder:** `drift_monitor.md` reads `auto_schedule_crew`'s LIVE decisions as its drift-meter input. You cannot wire `score_eval.py` to live output (next_action #2) until the output schema exists. This file *defines that schema* — so the next run can actually do the wiring.

## IMPORTANT — this is a DESIGN, not running code
Same honesty as `rollout_spec.md` and `drift_monitor.md`. `agent_surface.md` is unenforceable until: (1) `golden_v1.csv` `EXAMPLE-SYNTH` labels → human audited sign-off (next_action #1); (2) the six tools actually implemented; (3) Director signs both §7s. Until then it is the **target architecture** for the POC, not a deployed agent. The conceptual arc is now fully closed: R1 stack + R2 tool/agentic + R3 guardrails + R5 evals + R7 golden set + safe rollout + drift + **agent surface** = a buildable, trustworthy POC.

## Test (now operational, reuses the shipped spec)
Read `agent_surface.md` §2. A C&I project's TNB connection approval lapsed last week but `get_permit_status` still shows "cleared" because the sync job hasn't run. **(a)** Which single tool must catch this before `commit_schedule` runs, and what does it emit? **(b)** At dial = 0 (shadow), what actually happens to the schedule — and at dial = high (enforce), what is the ONE condition that lets `commit_schedule` auto-run? **(c)** Name the number that gates the dial promotion, and where it comes from.

*(Expected: (a) `flag_block_risk` → `should_block = Y`, escalate to human, STOP; (b) shadow = recommend only, human commits, never auto-override a block; enforce = auto-commit all ONLY if recall ≥ target on continuous live scoring; (c) recall on block cases from `score_eval.py` vs `golden_v1` — the ship-gate number from R5/R7, enforced by `rollout_spec.md` §3 auto-rollback.)*

## Observation of readiness
Genuine POC-build synthesis, not new math — re-composes R1+R2+R3+R5+R7 into a shippable shape. Student is stage 6 and has now shipped **all four** execution artifacts the ladder demanded (golden_v1+scorer, rollout_spec, drift_monitor, agent_surface). The real remaining gap is unchanged and is now the **prerequisite** to operating any of them: audit `EXAMPLE-SYNTH` labels (human) + implement the six tools + Director sign-off on three specs. Predicted grasp of the design: **high**; predicted ability to *operate* it: gated on the three prerequisites landing. The mentor's job of *teaching concepts* is effectively DONE — everything below is execution + sign-off, owned by `learn-pensolar`/human, not this cron.
