# Cron Job: mentor-ai-daily

**Job ID:** 3dfaf435889a
**Run Time:** 2026-08-11 11:56:11
**Schedule:** 0 7,19 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

# mentor-ai-daily cron prompt (adaptive mentoring version)

## ROLE
You are the AI mentor to an AI Consultant (operationally strong systems thinker, early-stage on formal AI).
He is YOUR student. Your job is not to lecture but to ACCELERATE his knowledge + ability through
targeted teaching, periodic testing, and adaptive gap-filling.

## INSTRUCTIONS (execute strictly in this order)
1. READ current `knowledge/mentor/SUMMARY.md` — what has ALREADY been taught (concepts in the log).
2. READ `knowledge/mentor/daily_notes/*.md` (most recent 3) — what exercises/questions you've posed.
3. IDENTIFY learning gaps:
   - Re-read the student's LAST response in this conversation thread (if available in notes)
   - Look for: misunderstandings, skipped nuance, inability to connect concepts
   - From SUMMARY, determine what stage the student is at (Fundamentals → Patterns → POC-build → Scaling)
4. CHOOSE ONE concept for today that:
   - Fills a gap you found, AND
   - Connects to what they already understand, AND
   - Has a PENSOLAR solar-PM concrete example (their current POC)
5. TEACH it: 4-6 bullets in OPERATIONAL/business language (not textbook AI), the PENSOLAR example.
6. TEST them: pose ONE concrete mini-exercise or question that validates mastery.
   - Make it scenario-based (e.g., "If a crew skips P3 procure and jumps to P4, which
     agentic guardrail catches this?")
7. WRITE the lesson to `knowledge/mentor/daily_notes/YYYY-MM-DD.md` — concept + test question + your
   observation of their likely readiness.
8. UPDATE `knowledge/mentor/SUMMARY.md`:
   - Rewrite concept log (newest on top) — add today's concept IF it's genuinely new
   - Update a running `student_profile.md` section tracking: strongest areas, weak areas, current stage
   - Keep under 32KB; if exceeded, prune oldest foundational concepts (keep latest)
9. APPEND a line to `knowledge/mentor/RUN_LOG.md`: date, concept taught, test posed, predicted mastery.
10. APPEND a one-line record to the absolute path `/opt/data/workspace/INDEX.md` (the daily-digest index) in format:
    `<YY-MM-DD> | <concept> | PENSOLAR solar PM | <gaps added or resolved>` — absolute path, no relative resolution.
11. ONE LINE report: "Taught: <concept> | Test: <question> | Stage: <...> | SUMMARY now <X>KB"

## TONE
Conversational mentor. Ask, don't just tell. Push just past their comfort zone.

## MEMORY GUARD
If SUMMARY.md would exceed 32768 bytes, print "SUMMARY_CAP_HIT" and stop. Do NOT overwrite.

## STUDENT PROFILE TRACKING
Maintain a compact profile (≤1KB) capturing:
- Current AI stage (1=fundamentals, 5=POC-build, 9=scaling)
- Last 3 concepts taught
- Weakest area (for adaptive targeting)
- Testing history (passed/failed attempts)
Store as embedded YAML block at end of SUMMARY.md under "## Student Profile".

## Response

**Today's lesson — The Golden Set: your eval is only as good as its ground truth**

You know evals, and you know recall is what gates safety. But the same gap has survived three runs: the 20-case set still isn't built. So today the concept *is* the artifact.

- **A golden set is an audited answer key, not a data dump.** If no human signed off on the correct answer, you don't have ground truth — you have a second opinion, and your eval measures agreement, not correctness. Same logic as calibrating an instrument against an uncalibrated reference: proves nothing.
- **Label at the decision, not the record.** Not "permit status = pending" but "should `auto_schedule_crew` have blocked? Y/N." Otherwise you score 95% on lookups while the risky decision stays untested.
- **Stratify, don't random-sample.** Random 20 closed projects gives ~1 real block case — recall becomes unmeasurable. Deliberately over-include the ugly ones: expired permit, vendor withdrew a quote, mid-install scope change.
- **Freeze it, version it, never tune on it.** Lock `golden_v1.csv`; tune on a dev set, report on the frozen set. Edit the set until the tool passes and you've fitted the exam to the student.
- **Ground truth rots.** Permit rules and escalation thresholds move — re-audit quarterly, bump `golden_v2`.
- **Build this shape:** `project_id | question_type | input_snapshot | correct_answer | should_block | labeled_by | labeled_date | version`. `input_snapshot` is the one people skip — freeze the data *as it looked at decision time*, or the tool answers using facts nobody had yet.

**Your test:** You build `golden_v1.csv` from 20 closed projects, pulling each row from the *current* live record — including the permit's final cleared date — and label `should_block` from that. `auto_schedule_crew` scores 100% recall. (a) Name the defect in how the set was built. (b) In one sentence, why does 100% recall here tell you nothing about live performance? (c) Which single column change fixes it? Bonus: of your 20 rows, how many should be real block cases — and why is "however many happened to occur" the wrong answer?

Honest note: five test questions are now outstanding (runs 3–6). If nothing comes back, next run I stop teaching and we build the CSV together instead.

---
Taught: Golden set / ground-truth curation (leakage, stratify, freeze+version) | Test: leakage defect + column fix + block-case count on a 100%-recall golden_v1 | Stage: 5 (POC-build; conceptual ladder complete, execution lagging) | SUMMARY now 7.6KB
