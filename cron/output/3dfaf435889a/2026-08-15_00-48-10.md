# Cron Job: mentor-ai-daily

**Job ID:** 3dfaf435889a
**Run Time:** 2026-08-15 00:48:10
**Schedule:** 0 7,11,15,19 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

[System: mentor-ai-daily prompt content]

## Response

**Taught: PENSOLAR `auto_schedule_crew` agent/tool surface (shipped as `agent_surface.md`) — the 4th and final execution artifact; concept-teaching arc now COMPLETE | Test: lapsed-permit sync gap → which tool catches it + dial-0 vs enforce behavior + which number gates promotion | Stage: 7 (POC execution; all 9 rungs taught) | SUMMARY now 18.6KB**

---

**Mentor note — 2026-08-15 (07:00 MYT slot)**

Bottom line: the mentor ladder is *done teaching*. All 9 rungs (stack → tool/agentic → guardrails → evals → precision/recall → golden set → safe rollout → drift → **agent surface**) are covered, and all 4 execution artifacts now exist as designs. Today I shipped the one the ladder kept referencing but never built: the actual POC shape for `auto_schedule_crew`.

**What shipped — `by_industry/solar_energy/pensolar/modules/agent_surface.md`:**
- **6 tools, guardrail-classed:** `get_permit_status` / `get_project_state` / `check_document_gate` (read-only, low risk) · `flag_block_risk` (THE guardrail — its `should_block` output *is* the `golden_v1.csv` column `score_eval.py` scores) · `propose_schedule` (advisory/reversible) · `commit_schedule` (the **only** irreversible action, recall-gated).
- **The agent loop:** goal = "install-ready on time without a compliance miss" → read state → check doc gate → flag block (escalate, **never** auto-override) → propose → act per dial → self-correct on slip. Predefined paths where possible, dynamic only for reschedule-on-slip (your R2 "start with tool use" rule).
- **Why it matters:** `drift_monitor.md` reads this agent's *live* decisions as its drift-meter input. You literally cannot wire `score_eval.py` to live output (your next_action #2) until the output schema exists — this file defines it. So the next run can finally do the wiring.
- **Honest prereq (same as the other specs):** it's a *target architecture*, not running code, until (1) `golden_v1.csv` labels are human-audited, (2) the 6 tools are implemented, (3) Director signs all three §7/sign-off blocks.

**Test (reuses the shipped spec):**
A C&I project's TNB approval lapsed last week but `get_permit_status` still shows "cleared" (sync job hasn't run). **(a)** Which single tool must catch this before `commit_schedule` runs, and what does it emit? **(b)** At dial 0 (shadow) what happens to the schedule; at dial high (enforce) what's the one condition letting `commit_schedule` auto-run? **(c)** Name the number that gates the dial promotion and where it comes from.
*(Expected: `flag_block_risk`→`should_block=Y`, escalate/STOP; shadow=recommend only, human commits; enforce=auto-commit all iff recall≥target on continuous live scoring; the gate number = recall on block cases from `score_eval.py` vs `golden_v1`, enforced by `rollout_spec.md` §3.)*

**Readiness:** You've now shipped all four artifacts the ladder demanded. Predicted grasp of the design — **high**. Ability to *operate* it stays gated on the three prerequisites above, which are execution + Director sign-off owned by `learn-pensolar`/you, not this cron. **From here, the mentor's job is teaching — finished. Everything left is build + audit + sign-off.**
