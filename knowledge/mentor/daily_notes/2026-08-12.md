# Mentor Daily Note — 2026-08-12 (03:00 MYT run)

> **Adaptive override triggered.** SUMMARY.next_action = *"if tests still unanswered next run, STOP new concepts and co-build the CSV."* The student is a cron target (no interactive answers — Runs 3–6 tests still pending), and the standing gap was pure execution: `golden_v1.csv` was teased in 4 consecutive runs but never built. So today we **shipped the artifact** instead of a 5th teaser.

## What we built (the gap, closed)
- `by_industry/solar_energy/pensolar/modules/golden_v1.csv` — 20 rows using the exact 8-column schema from Run 6: `project_id | question_type | input_snapshot | correct_answer | should_block | labeled_by | labeled_date | version`.
- `input_snapshot` is frozen **AT DECISION TIME** (2026-03-04), not the final cleared date — that is the leakage trap from Run 6, demonstrated in the data rather than just named.
- **Stratified on purpose:** 5 of 20 rows (25%) are real block cases — expired permit, vendor withdrew quote, scope change mid-install, permit pending past scheduled date, open authority objection. A random 20 would carry ~1; this is what makes recall measurable.
- `score_eval.py` — ~20-line scorer. Run `python3 score_eval.py golden_v1.csv <your_tool_output.csv>` → prints the 2×2 + precision/recall/accuracy. Verified run on the sample: TP=4 FP=1 FN=1 TN=14, precision 80%, recall 80%, accuracy 90%.

## IMPORTANT — this is a SCAFFOLD, not ground truth
Every `labeled_by` = `EXAMPLE-SYNTH`. The rows are realistic PENSOLAR patterns to show you what a *correct row looks like* — they are **NOT** audited sign-off. The whole Run-6 lesson: no human sign-off = no ground truth = you are measuring agreement, not correctness. Before this becomes a real eval you must replace `correct_answer` + `should_block` on every row with values a human (you / the PM) signed off on, set `labeled_by` to a real name + date, and lock the file as `golden_v1.csv`.

## Test (now operational, not recall)
Point your real `auto_schedule_crew` logic (or a manual pass over 20 closed projects) at the schema and produce `predictions.csv` with `project_id,should_block`. Run `python3 score_eval.py golden_v1.csv predictions.csv`. Report back:
(a) your TP / FP / FN / TN,
(b) the single number you would write into the safety spec as the ship gate,
(c) if recall < 100%, which block case your logic missed and why.

## Observation of readiness
Conceptual ladder is complete (stack → tool-vs-agentic → guardrails → evals → precision/recall → golden set). The execution artifact now exists; the remaining work is **audit sign-off + wiring the tool's live output into the scorer**. Predicted next state: stage 5 (POC-build) → 6 (scaling) once the locked set is fed by real tool output and the recall gate is enforced in the spec.
